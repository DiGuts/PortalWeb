Options -MultiViews
RewriteEngine On

# Route all /api/* requests through the PHP front-controller
RewriteRule ^api/.*$ api/index.php [QSA,L]

# React SPA fallback for everything else
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.html [QSA,L]
